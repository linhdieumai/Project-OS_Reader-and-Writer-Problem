#Cách chạy

- tải hết cả 4 file ở main về (nhớ để trong cùng 1 folder nào đó, ví dụ như chung trong download)
- chỉ chạy maincheck.py

#Prob
- Xem cái read_count nhảy nhanh như thế là có đúng hay không
- E hằng xem thử muốn thống nhất tên một số biến giống bản reader và writer không (VD: read_count_lock và read_m,...)
